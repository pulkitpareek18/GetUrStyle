# SMTP Configuration for Contact Form
# Add these environment variables in your Vercel dashboard:
# Project Settings > Environment Variables

# SMTP Host (e.g., smtp.gmail.com, smtp.office365.com)
SMTP_HOST=smtp.gmail.com

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Set to "true" for SSL (port 465), "false" for TLS (port 587)
SMTP_SECURE=false

# Your email address (for Gmail, use your full email)
SMTP_USER=your-email@gmail.com

# Your email password or App Password
# For Gmail: Create an App Password at https://myaccount.google.com/apppasswords
SMTP_PASS=your-app-password

# From address (usually same as SMTP_USER)
SMTP_FROM=your-email@gmail.com

# Recipient email (where form submissions go)
SMTP_TO=geturstyleofficial@gmail.com
